<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.project"> <!-- Replace this with your actual package name -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PROJECT"
        tools:targetApi="31">

        <!-- Main activity to launch the app -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Enter Name Activity -->
        <activity
            android:name=".EnterNameActivity"
            android:exported="false" /> <!-- Ensure the name is correct here -->

        <!-- Welcome Back Activity -->
        <activity
            android:name=".WelcomeBackActivity"
            android:exported="false" /> <!-- Ensure the name is correct here -->

        <!-- Lessons List Activity -->
        <activity
            android:name=".LessonsListActivity"
            android:exported="false" /> <!-- Ensure the name is correct here -->

        <!-- Lesson Details Activity -->
        <activity
            android:name=".LessonDetailsActivity"
            android:exported="false" /> <!-- Ensure the name is correct here -->

    </application>

</manifest>
